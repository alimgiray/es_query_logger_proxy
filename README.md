# How it works?
It opens the `8013` port and forwards all the request to `localhost:9200` and on the meanwhile prints the called URL and body of the request.


Usefull to debug your ElasticSearch queries during development.
